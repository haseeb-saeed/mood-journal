<!DOCTYPE html>
<html>
<head>
    <title><%= post.title %> - Mood Journal</title>
</head>

<body>
    <h2><%= post.title %></h2>
    <p><%= post.body %></p>

    <form action='/posts/<%= post.id %>' method="post">
        <input type="hidden" name="_method" value="DELETE"/>
        <button type="submit">Delete</button>
    </form>
</body>
</html>
